#include<stdio.h>
int main()
{
int i=0,a;
printf("enter the size of array");
scanf("%d",&a);
int arr[a];
printf("\nenter the elments of array");
for(i=0;i<a;i++)
{scanf("%d",&arr[i]);

}
printf("the sorted array is")
for(i=0;i<a;i++)
{printf("%d",arr[i]);

}
return 0;
}
